import React from 'react';
import {StyleSheet, View, Text} from 'react-native';

const Tips = () => {
  return (
    <View>
      <Text>Tips hi</Text>
    </View>
  );
};

const styles = StyleSheet.create({
  block: {},
});

export default Tips;
